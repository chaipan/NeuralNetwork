import tensorflow as tf

"""
max_pool(value, ksize, strides, padding, data_format="NHWC", name=None):
value:输入值[batch, height, width, channel]
ksize:输入维度上每个值得大小，一般在batch和channel上取1[1,size,size,1],size一般取2
stride:步调，每个维度上的步调[1,stride,stride,1]

"""



